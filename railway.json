{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "moltis --bind 0.0.0.0 --port $PORT --no-tls",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 5,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
